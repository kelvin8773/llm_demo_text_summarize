[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --tb=short --disable-warnings --color=yes
markers =
    unit: Unit tests for individual functions
    integration: Integration tests for complete workflows
    slow: Tests that take longer to run
    performance: Performance and benchmark tests
    visualization: Tests involving matplotlib plotting
    chinese: Tests specific to Chinese language processing
    english: Tests specific to English language processing
minversion = 6.0
timeout = 300
log_cli = true
log_cli_level = INFO
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
    ignore::UserWarning:matplotlib.*
    ignore::UserWarning:sklearn.*
    ignore::UserWarning:transformers.*